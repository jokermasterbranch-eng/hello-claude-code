# hello-claude-code

A tiny starter project used to practice the GitHub workflow: clone, branch, commit, and open a pull request.

## What's here

- `greet.py` — a small script that prints a friendly greeting for a given name.

## Usage

```bash
python greet.py Ada
```
